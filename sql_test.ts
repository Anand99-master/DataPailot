import { SQLServerAdapter } from './server/database/SQLServerAdapter';

// We can't really run it against a real SQL server easily unless we mock it or use an existing test. Let's just look at the adapter code.
